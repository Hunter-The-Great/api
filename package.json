{
  "name": "api",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts",
    "db-gen": "bun x prisma generate",
    "db-push": "bun x prisma db push",
    "format": "prettier --write \"**/*.ts\""
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "@fastify/cors": "^10.1.0",
    "@fastify/websocket": "^11.0.2",
    "@prisma/client": "^5.22.0",
    "@sentry/node": "^9.13.0",
    "fastify": "^5.3.2",
    "posthog-node": "^4.12.0",
    "zod": "^3.24.3"
  }
}
